<configuration>

    <appender name="SERVER" class="ch.qos.logback.core.FileAppender">
        <file>server.log</file>

        <encoder>
            <pattern>%date [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CLIENT" class="ch.qos.logback.core.FileAppender">
        <file>client.log</file>
        <encoder>
            <pattern>%date [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern> %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE_X" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern> XX %date --> %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="client">
<!--        <appender-ref ref="CLIENT" />-->
        <appender-ref ref="CONSOLE_X"/>
    </logger>

    <logger name="server">
        <appender-ref ref="SERVER" />
    </logger>

    <root level="none" >
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
